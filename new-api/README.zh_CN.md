cd new-api
go run main.go

cd new-api/web
bun run dev


ofoxai文章生成 HTML提示词
生成精简内联样式版 HTML，所有样式均写在元素内部，无外部 CSS 或 <style> 标签。一个 <div> 包裹全部内容，div不用加样式，div里面的加，div不要meta，不要article-tags，不要data-astro-cid-bvzihdzo